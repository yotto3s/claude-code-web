FROM ubuntu:24.04

# Install tools including Python for password verification and Node.js
RUN apt-get update && apt-get install -y --no-install-recommends \
    bash \
    whois \
    curl \
    python3 \
    nodejs \
    npm \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Copy package files
COPY package*.json ./

# Remove native modules not needed in gateway (node-pty, better-sqlite3, claude-agent-sdk)
RUN node -e "const p=require('./package.json'); delete p.dependencies['node-pty']; delete p.dependencies['better-sqlite3']; delete p.dependencies['@anthropic-ai/claude-agent-sdk']; require('fs').writeFileSync('package.json', JSON.stringify(p, null, 2))"

# Install dependencies
RUN npm install --production

# Copy application files
COPY gateway.js ./
COPY src/ ./src/
COPY public/ ./public/

# Create data directory
RUN mkdir -p /app/data && chmod 777 /app/data

# Expose port
EXPOSE 3000

# Start gateway
CMD ["node", "gateway.js"]
