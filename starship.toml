# Starship Configuration for Claude Code Web Terminal

format = """
[┌───────────────────────────────────────────>](bold green)
[│](bold green)$username\
$hostname\
$directory\
$git_branch\
$git_status\
$nodejs\
$python\
$docker_context\
$cmd_duration
[└─>](bold green) """

[username]
style_user = "bold blue"
style_root = "bold red"
format = "[$user]($style) "
show_always = true

[hostname]
ssh_only = false
format = "[@$hostname](bold yellow) "
disabled = false

[directory]
truncation_length = 3
truncate_to_repo = true
format = "[$path]($style)[$read_only]($read_only_style) "
style = "bold cyan"

[git_branch]
symbol = "🌱 "
format = "[$symbol$branch]($style) "
style = "bold purple"

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "bold red"

[nodejs]
symbol = "⬢ "
format = "[$symbol($version )]($style)"
style = "bold green"

[python]
symbol = "🐍 "
format = '[${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
style = "bold yellow"

[docker_context]
symbol = "🐳 "
format = "[$symbol$context]($style) "
style = "bold blue"

[cmd_duration]
min_time = 500
format = "[took $duration](bold yellow) "

[character]
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"
